apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
   - port: 5434
     targetPort: 5432
     nodePort: 31079
  selector:
   app: postgres
